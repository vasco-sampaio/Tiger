let
  type one = { tail : two }
  type two = { tail : one }
  function one(t : two) : one
     = one { tail = t }
  function two(t : one) : two
     = two { tail = t }
  var one := one(two(nil))
in
end
